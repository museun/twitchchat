[package]
name = "twitchchat_smol"
version = "0.1.0"
authors = ["museun <museun@outlook.com>"]
edition = "2018"

[features]
rustls = ["async-tls"]

[dependencies]
twitchchat = "0.14.5"
smol = "1.2.4"
async-tls = { version = "0.10.0", optional = true }

[dev-dependencies]
futures-io = "0.3.6"
